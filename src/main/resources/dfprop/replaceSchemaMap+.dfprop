map:{
    ; additionalUserMap = map:{
        ; system = map:{
            ; url = @url@
            ; schema = @schema@
            ; user = @user@
            ; password = @password@
        }
    }
}
