// #gradle8 "task wrapper" 定義ができなくなったのでwrapperブロックを使うように by jflute (2024/11/02)
wrapper {
    gradleVersion = versions.gradle
}

tasks.withType(SourceTask) each {
    try {
        it.options.encoding = encoding
    } catch (ignore) {
    }
}
